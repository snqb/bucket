FROM node:22-alpine
WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN corepack enable && pnpm install --prod --frozen-lockfile

COPY server.js .

ENV PORT=8040
ENV DATA_DIR=/data

EXPOSE 8040
CMD ["node", "server.js"]
